name: 🌐 Web Builds
on:
  workflow_call:

# Global Settings
env:
  SCONS_FLAGS: >-
    dev_mode=yes
    tests=no
    debug_symbols=no
    use_closure_compiler=yes
  EM_VERSION: 4.0.11

jobs:
  web-template:
    runs-on: ubuntu-24.04
    name: ${{ matrix.name }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Template w/ threads (target=template_release, threads=yes)
            cache-name: web-template
            target: template_release
            scons-flags: threads=yes
            artifact: true

          - name: Template w/o threads (target=template_release, threads=no)
            cache-name: web-nothreads-template
            target: template_release
            scons-flags: threads=no
            artifact: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Emscripten latest
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: ${{ env.EM_VERSION }}
          no-cache: true

      - name: Verify Emscripten setup
        run: |
          emcc -v

<<<<<<< HEAD
      - name: Restore Godot build cache
        uses: ./.github/actions/godot-cache-restore
=======
      - name: Verify Key Transfer
        run: |
          if [ -z "$SCRIPT_AES256_ENCRYPTION_KEY" ]; then
            echo "错误：SCRIPT_AES256_ENCRYPTION_KEY 环境变量未设置"
            exit 1
          fi
          
          key="$SCRIPT_AES256_ENCRYPTION_KEY"
          
          if [ ${#key} -ne 64 ]; then
            echo "错误：环境变量中的密钥长度不正确"
            exit 1
          fi
          
          masked="${key:0:8}...${key: -8}"
          echo "环境变量验证通过：$masked (长度: ${#key})"
          echo "密钥传递成功，准备开始构建..."

      - name: Build ${{ matrix.target }}
        run: |
          echo "开始构建 ${{ matrix.target }}..."
          echo "使用加密密钥构建模板（支持加密PCK解密）"
          
          scons platform=web target=${{ matrix.target }} threads=yes script_encryption_key="$SCRIPT_AES256_ENCRYPTION_KEY"

      - name: Verify Build Output
        run: |
          binPath="bin/"
          if [ ! -d "$binPath" ]; then
            echo "错误：构建目录不存在"
            exit 1
          fi
          
          files=$(find "$binPath" -type f)
          if [ -z "$files" ]; then
            echo "错误：未找到构建产物"
            exit 1
          fi
          
          echo "构建产物列表："
          for f in $files; do
            size=$(du -h "$f" | cut -f1)
            echo "  - $(basename $f) ($size)"
          done

      - name: Prepare artifacts (exclude obj)
        run: |
          rm -rf bin/obj || true
          ls -l bin/

      - name: Upload ${{ matrix.target }} artifact
        uses: actions/upload-artifact@v4
>>>>>>> def084d67d (expect obj/ & fix apk ipa)
        with:
          cache-name: ${{ matrix.cache-name }}
        continue-on-error: true

      - name: Setup Python and SCons
        uses: ./.github/actions/godot-deps

      - name: Compilation
        uses: ./.github/actions/godot-build
        with:
          scons-flags: ${{ env.SCONS_FLAGS }} ${{ matrix.scons-flags }}
          platform: web
          target: ${{ matrix.target }}

      - name: Save Godot build cache
        uses: ./.github/actions/godot-cache-save
        with:
          cache-name: ${{ matrix.cache-name }}
        continue-on-error: true

      - name: Upload artifact
        uses: ./.github/actions/upload-artifact
        if: matrix.artifact
        with:
          name: ${{ matrix.cache-name }}
